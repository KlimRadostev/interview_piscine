#include <stdint.h>
#include <stddef.h>
#include <string.h>
#include <stdbool.h>
#include <stdio.h>

int main(int ac, char *argv[])
{
    /* import libft helper library */

    FT_STATUS status = FT_STATUS_SUCCESS;

    if (ac != 4) {
        return -1;
    }

    uint8_t op = get_op(argv[2], );
    FT_STATUS = 

    return 0;
}