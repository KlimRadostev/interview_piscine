#include "header.h"
#include <stdint.h>
#include <stddef.h>

int toInt(char *bits)
{

}

char *getSum(char *a, char *b)
{
    char ret[6] = {0};
    char holder = 0;

    for (size_t i = 0; i < 6; i++)
    {
        // if all 3 are 1
        if (a[i] == '1' && b[i] == '1')
    }
    return (ret);
}



int main()
{
    return (0);
}